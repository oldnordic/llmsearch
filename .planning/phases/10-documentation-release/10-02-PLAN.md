---
phase: 10-documentation-release
plan: 02
type: execute
depends_on: []
files_modified: [examples/]
autonomous: true
---

<objective>
Create example usage patterns directory with runnable examples.

Purpose: Provide concrete examples demonstrating common llmsearch workflows.
Output: New examples/ directory with shell scripts and expected outputs
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create examples directory structure</name>
  <files>examples/basic-search.sh, examples/json-output.sh, examples/glob-filtering.sh</files>
  <action>Create examples/ directory with 3 shell scripts:

1. **examples/basic-search.sh**: Basic pattern matching demo
   - Search for common patterns like "fn main", "struct", "impl"
   - Show default output format
   - Include comments explaining what's happening

2. **examples/json-output.sh**: JSON mode demonstration
   - Show --json flag usage
   - Pipe output to jq for formatting
   - Demonstrate execution_id tracking
   - Show match fields (file, byte_start, byte_end, line_number, column_number)

3. **examples/glob-filtering.sh**: File filtering examples
   - Single glob: -g '*.rs'
   - Multiple globs: -g '*.rs' -g '*.toml'
   - Complex patterns: 'test_*.rs'

Each script should be:
- Executable (chmod +x)
- Start with #!/usr/bin/env bash
- Have comments explaining each command
- Self-contained (work on the llmsearch codebase itself)</action>
  <verify>All scripts are executable and run without errors on the project</verify>
  <done>Examples directory created with 3 working shell scripts</done>
</task>

<task type="auto">
  <name>Task 2: Add README for examples directory</name>
  <files>examples/README.md</files>
  <action>Create examples/README.md explaining:

1. Purpose of examples directory
2. How to run each script (./examples/basic-search.sh)
3. What each example demonstrates
4. Prerequisites (llmsearch built: cargo build --release)
5. Expected output notes (paths will vary by system)

Keep it brief (~30 lines).</action>
  <verify>examples/README.md exists and documents all scripts</verify>
  <done>Examples are self-documenting</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] examples/ directory created
- [ ] 3 shell scripts present and executable
- [ ] examples/README.md explains usage
- [ ] All scripts run successfully on the project
- [ ] Scripts are well-commented
</verification>

<success_criteria>

- examples/ directory with 3 runnable scripts
- Each script demonstrates a different aspect of llmsearch
- examples/README.md provides usage guide
- Users can learn by running examples
  </success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-release/10-02-SUMMARY.md`
</output>
