---
phase: 10-documentation-release
plan: 03
type: execute
depends_on: []
files_modified: [Cargo.toml]
autonomous: true
---

<objective>
Package llmsearch for release with version verification and build validation.

Purpose: Ensure the project builds correctly, version is set, and all artifacts are ready for release.
Output: Validated release build, version tag ready
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify version in Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>Check Cargo.toml version field. For v1.0.0 release:

1. If version is "0.1.0", update to "1.0.0" (this is a complete, production-ready tool)
2. Add optional metadata fields if not present:
   - description: Brief one-liner
   - license: "MIT"
   - repository: URL if available
   - authors: Your name/email
   - keywords: ["search", "cli", "llm", "code-search"]

Keep changes minimal - only add what's missing.</action>
  <verify>cargo build --release succeeds with updated Cargo.toml</verify>
  <done>Version is appropriately set for initial release</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite</name>
  <files></files>
  <action>Execute comprehensive release verification:

1. Run unit tests: cargo test
2. Run integration tests: cargo test --test cli
3. Run determinism tests: cargo test --test determinism
4. Verify all 29 tests pass
5. Build release binary: cargo build --release
6. Verify binary works: ./target/release/llmsearch --help

This confirms the tool is fully functional before release.</action>
  <verify>All 29 tests pass and release binary runs correctly</verify>
  <done>Test suite validates release readiness</done>
</task>

<task type="auto">
  <name>Task 3: Create RELEASE.md notes</name>
  <files>RELEASE.md</files>
  <action>Create RELEASE.md with:

1. **Version**: 1.0.0
2. **Release date**: Current date
3. **What's included**: All 10 phases complete
4. **Features**: Bullet list of capabilities
5. **Installation**: cargo install --path . or cargo install llmsearch (when published)
6. **Known limitations**: Linux-only, text files only
7. **Future work**: Potential enhancements (not commitments)

Keep it brief and focused on what users need to know about this release (~50 lines).</action>
  <verify>RELEASE.md exists and contains release information</verify>
  <done>Release notes document v1.0.0</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Cargo.toml has appropriate version and metadata
- [ ] All 29 tests pass (cargo test)
- [ ] Release binary builds successfully (cargo build --release)
- [ ] RELEASE.md created with version notes
- [ ] No warnings or errors in build output
</verification>

<success_criteria>

- Release artifacts ready
- Version properly set in Cargo.toml
- All tests passing
- RELEASE.md documents v1.0.0
- Project is ready for publication to crates.io
  </success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-release/10-03-SUMMARY.md`
</output>
