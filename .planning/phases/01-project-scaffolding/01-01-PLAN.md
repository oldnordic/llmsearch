---
phase: 01-project-scaffolding
plan: 01
type: execute
depends_on: []
files_modified: [Cargo.toml]
autonomous: true
---

<objective>
Initialize Rust project with Cargo and add core dependencies.

Purpose: Create the foundation for llmsearch - a binary Rust project with necessary dependencies for CLI parsing, file walking, regex, UUID generation, and JSON serialization.
Output: Working Cargo project with Cargo.toml configured
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# First phase - no prior summaries
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cargo binary project</name>
  <files>Cargo.toml</files>
  <action>Run `cargo init --name llmsearch` to create a new binary Rust project. This creates:
  - Cargo.toml with basic binary configuration
  - src/main.rs with hello world
  - .gitignore for Rust projects

Do NOT add any initial dependencies yet - we'll add them in Task 2.
</action>
  <verify>Cargo.toml exists with name = "llmsearch" and [[bin]] section; src/main.rs exists with fn main()</verify>
  <done>`cargo build` succeeds with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Add core dependencies to Cargo.toml</name>
  <files>Cargo.toml</files>
  <action>Add the following dependencies to Cargo.toml under [dependencies]:
- clap = { version = "4.5", features = ["derive"] }  # CLI argument parsing
- ignore = "0.4"  # .gitignore-aware file walking
- regex = "1.10"  # pattern matching
- serde = { version = "1.0", features = ["derive"] }  # JSON serialization
- serde_json = "1.0"  # JSON output
- uuid = { version = "1.6", features = ["v4", "serde"] }  # execution/match IDs

Use these exact versions (or compatible minor versions) to ensure compatibility.
Do NOT add dev-dependencies yet - tests come in Phase 9.
</action>
  <verify>`cargo check` succeeds after adding dependencies</verify>
  <done>All dependencies resolved and cached by cargo</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build` succeeds with no errors
- [ ] `cargo run -- --help` shows clap's default help (from unmodified main.rs)
- [ ] Cargo.lock exists with dependencies resolved
</verification>

<success_criteria>
- Cargo project initialized as binary
- All 6 dependencies added and compiling
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding/01-01-SUMMARY.md`
</output>
